import { ThemedText } from '../ThemedText';
import { ThemedView } from '../ThemedView';

export const CreditUsageTab = () => {
  return (
    <ThemedView>
      <ThemedText>크레딧 사용 내역</ThemedText>
    </ThemedView>
  );
};
