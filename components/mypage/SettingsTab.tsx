import { ThemedText } from '../ThemedText';
import { ThemedView } from '../ThemedView';

export const SettingsTab = () => {
  return (
    <ThemedView>
      <ThemedText>환경설정</ThemedText>
    </ThemedView>
  );
};
